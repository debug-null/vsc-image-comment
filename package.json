{
	"name": "paste-image-comment",
	"displayName": "Paste image comment",
	"description": "Add image comments to any of your code",
	"keywords": [
		"Paste Image comment",
		"comment",
		"paste comment",
		"phote annotate",
		"annotate"
	],
	"version": "1.0.2",
	"publisher": "pseudo",
	"engines": {
		"vscode": "^1.67.0"
	},
	"categories": [
		"Other"
	],
	"license": "MIT",
	"activationEvents": [
		"onStartupFinished"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/debug-null/vsc-image-comment.git"
	},
	"icon": "images/icon.png",
	"main": "./dist/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "PasteImageComment.remove",
				"title": "%PasteImageComment.remove%"
			},
			{
				"command": "PasteImageComment.paste",
				"title": "%PasteImageComment.paste%"
			}
		],
		"keybindings": [
			{
				"command": "PasteImageComment.paste",
				"key": "ctrl+alt+v",
				"mac": "cmd+alt+v",
				"when": "editorTextFocus"
			}
		],
		"configuration": {
			"type": "object",
			"title": "PasteImageComment",
			"properties": {
				"PasteImageComment.imgSavePath": {
					"type": "string",
					"description": "%PasteImageComment.imgSavePath%"
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run package",
		"compile": "webpack",
		"watch": "webpack --watch",
		"package": "webpack --mode production --devtool hidden-source-map",
		"compile-tests": "tsc -p . --outDir out",
		"watch-tests": "tsc -p . -w --outDir out",
		"pretest": "npm run compile-tests && npm run compile && npm run lint",
		"lint": "eslint src --ext ts",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/fs-extra": "^9.0.13",
		"@types/glob": "^7.2.0",
		"@types/mocha": "^9.1.1",
		"@types/node": "14.x",
		"@types/vscode": "^1.67.0",
		"@typescript-eslint/eslint-plugin": "^5.21.0",
		"@typescript-eslint/parser": "^5.21.0",
		"@vscode/test-electron": "^2.1.3",
		"eslint": "^8.14.0",
		"glob": "^8.0.1",
		"mocha": "^9.2.2",
		"ts-loader": "^9.2.8",
		"typescript": "^4.6.4",
		"webpack": "^5.70.0",
		"webpack-cli": "^4.9.2"
	},
	"dependencies": {
		"fs-extra": "^10.1.0"
	}
}